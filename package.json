{
  "name": "agentstack",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "test": "turbo test && node --test scripts/install.test.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.5.1",
      "onFail": "download"
    }
  },
  "packageManager": "pnpm@12.5.1",
  "devDependencies": {
    "turbo": "^2.10.4"
  }
}
